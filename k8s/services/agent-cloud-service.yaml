apiVersion: v1
kind: Service
metadata:
  name: agent-cloud-service
  namespace: embodied-ai
spec:
  selector:
    app: agent-cloud
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
