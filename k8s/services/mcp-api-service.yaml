apiVersion: v1
kind: Service
metadata:
  name: mcp-api-service
  namespace: embodied-ai
spec:
  selector:
    app: mcp-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
