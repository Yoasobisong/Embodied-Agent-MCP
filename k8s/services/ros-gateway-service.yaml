apiVersion: v1
kind: Service
metadata:
  name: ros-gateway-service
  namespace: embodied-ai
spec:
  selector:
    app: ros-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
